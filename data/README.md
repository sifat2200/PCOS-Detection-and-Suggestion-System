# Dataset

The project expects:

```text
pcos_prediction_dataset.csv
```

The dataset is intentionally not included in the repository by default. Before publishing the dataset, verify the original source's license and redistribution permissions.

For Google Colab, upload the CSV using the Files panel. For local execution, place the CSV beside the notebook or update the loader path.
